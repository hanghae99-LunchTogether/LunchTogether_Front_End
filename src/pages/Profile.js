import React from "react";

import UserProfile from "../components/UserProfile";

const Profile = (props) => {
  return (
    <React.Fragment>
      <UserProfile />
    </React.Fragment>
  );
};

export default Profile;
